/*
 * This file is generated by jOOQ.
 */
package io.zeitwert.fm.contact.model.db.tables;


import io.zeitwert.fm.contact.model.db.Keys;
import io.zeitwert.fm.contact.model.db.Public;
import io.zeitwert.fm.contact.model.db.tables.CodeGender.CodeGenderPath;
import io.zeitwert.fm.contact.model.db.tables.ObjContact.ObjContactPath;
import io.zeitwert.fm.contact.model.db.tables.records.CodeSalutationRecord;

import java.util.Arrays;
import java.util.Collection;
import java.util.List;

import org.jooq.Condition;
import org.jooq.Field;
import org.jooq.ForeignKey;
import org.jooq.InverseForeignKey;
import org.jooq.Name;
import org.jooq.Path;
import org.jooq.PlainSQL;
import org.jooq.QueryPart;
import org.jooq.Record;
import org.jooq.SQL;
import org.jooq.Schema;
import org.jooq.Select;
import org.jooq.Stringly;
import org.jooq.Table;
import org.jooq.TableField;
import org.jooq.TableOptions;
import org.jooq.UniqueKey;
import org.jooq.impl.DSL;
import org.jooq.impl.SQLDataType;
import org.jooq.impl.TableImpl;


/**
 * This class is generated by jOOQ.
 */
@SuppressWarnings({ "all", "unchecked", "rawtypes", "this-escape" })
public class CodeSalutation extends TableImpl<CodeSalutationRecord> {

    private static final long serialVersionUID = 1L;

    /**
     * The reference instance of <code>public.code_salutation</code>
     */
    public static final CodeSalutation CODE_SALUTATION = new CodeSalutation();

    /**
     * The class holding records for this type
     */
    @Override
    public Class<CodeSalutationRecord> getRecordType() {
        return CodeSalutationRecord.class;
    }

    /**
     * The column <code>public.code_salutation.id</code>.
     */
    public final TableField<CodeSalutationRecord, String> ID = createField(DSL.name("id"), SQLDataType.VARCHAR(40).nullable(false), this, "");

    /**
     * The column <code>public.code_salutation.name</code>.
     */
    public final TableField<CodeSalutationRecord, String> NAME = createField(DSL.name("name"), SQLDataType.VARCHAR(100).nullable(false), this, "");

    /**
     * The column <code>public.code_salutation.gender_id</code>.
     */
    public final TableField<CodeSalutationRecord, String> GENDER_ID = createField(DSL.name("gender_id"), SQLDataType.VARCHAR(40).nullable(false), this, "");

    private CodeSalutation(Name alias, Table<CodeSalutationRecord> aliased) {
        this(alias, aliased, (Field<?>[]) null, null);
    }

    private CodeSalutation(Name alias, Table<CodeSalutationRecord> aliased, Field<?>[] parameters, Condition where) {
        super(alias, null, aliased, parameters, DSL.comment(""), TableOptions.table(), where);
    }

    /**
     * Create an aliased <code>public.code_salutation</code> table reference
     */
    public CodeSalutation(String alias) {
        this(DSL.name(alias), CODE_SALUTATION);
    }

    /**
     * Create an aliased <code>public.code_salutation</code> table reference
     */
    public CodeSalutation(Name alias) {
        this(alias, CODE_SALUTATION);
    }

    /**
     * Create a <code>public.code_salutation</code> table reference
     */
    public CodeSalutation() {
        this(DSL.name("code_salutation"), null);
    }

    public <O extends Record> CodeSalutation(Table<O> path, ForeignKey<O, CodeSalutationRecord> childPath, InverseForeignKey<O, CodeSalutationRecord> parentPath) {
        super(path, childPath, parentPath, CODE_SALUTATION);
    }

    /**
     * A subtype implementing {@link Path} for simplified path-based joins.
     */
    public static class CodeSalutationPath extends CodeSalutation implements Path<CodeSalutationRecord> {

        private static final long serialVersionUID = 1L;
        public <O extends Record> CodeSalutationPath(Table<O> path, ForeignKey<O, CodeSalutationRecord> childPath, InverseForeignKey<O, CodeSalutationRecord> parentPath) {
            super(path, childPath, parentPath);
        }
        private CodeSalutationPath(Name alias, Table<CodeSalutationRecord> aliased) {
            super(alias, aliased);
        }

        @Override
        public CodeSalutationPath as(String alias) {
            return new CodeSalutationPath(DSL.name(alias), this);
        }

        @Override
        public CodeSalutationPath as(Name alias) {
            return new CodeSalutationPath(alias, this);
        }

        @Override
        public CodeSalutationPath as(Table<?> alias) {
            return new CodeSalutationPath(alias.getQualifiedName(), this);
        }
    }

    @Override
    public Schema getSchema() {
        return aliased() ? null : Public.PUBLIC;
    }

    @Override
    public UniqueKey<CodeSalutationRecord> getPrimaryKey() {
        return Keys.CODE_SALUTATION_PKEY;
    }

    @Override
    public List<ForeignKey<CodeSalutationRecord, ?>> getReferences() {
        return Arrays.asList(Keys.CODE_SALUTATION__CODE_SALUTATION_GENDER_ID_FKEY);
    }

    private transient CodeGenderPath _codeGender;

    /**
     * Get the implicit join path to the <code>public.code_gender</code> table.
     */
    public CodeGenderPath codeGender() {
        if (_codeGender == null)
            _codeGender = new CodeGenderPath(this, Keys.CODE_SALUTATION__CODE_SALUTATION_GENDER_ID_FKEY, null);

        return _codeGender;
    }

    private transient ObjContactPath _objContact;

    /**
     * Get the implicit to-many join path to the <code>public.obj_contact</code>
     * table
     */
    public ObjContactPath objContact() {
        if (_objContact == null)
            _objContact = new ObjContactPath(this, null, Keys.OBJ_CONTACT__OBJ_CONTACT_SALUTATION_ID_FKEY.getInverseKey());

        return _objContact;
    }

    @Override
    public CodeSalutation as(String alias) {
        return new CodeSalutation(DSL.name(alias), this);
    }

    @Override
    public CodeSalutation as(Name alias) {
        return new CodeSalutation(alias, this);
    }

    @Override
    public CodeSalutation as(Table<?> alias) {
        return new CodeSalutation(alias.getQualifiedName(), this);
    }

    /**
     * Rename this table
     */
    @Override
    public CodeSalutation rename(String name) {
        return new CodeSalutation(DSL.name(name), null);
    }

    /**
     * Rename this table
     */
    @Override
    public CodeSalutation rename(Name name) {
        return new CodeSalutation(name, null);
    }

    /**
     * Rename this table
     */
    @Override
    public CodeSalutation rename(Table<?> name) {
        return new CodeSalutation(name.getQualifiedName(), null);
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public CodeSalutation where(Condition condition) {
        return new CodeSalutation(getQualifiedName(), aliased() ? this : null, null, condition);
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public CodeSalutation where(Collection<? extends Condition> conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public CodeSalutation where(Condition... conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public CodeSalutation where(Field<Boolean> condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public CodeSalutation where(SQL condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public CodeSalutation where(@Stringly.SQL String condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public CodeSalutation where(@Stringly.SQL String condition, Object... binds) {
        return where(DSL.condition(condition, binds));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public CodeSalutation where(@Stringly.SQL String condition, QueryPart... parts) {
        return where(DSL.condition(condition, parts));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public CodeSalutation whereExists(Select<?> select) {
        return where(DSL.exists(select));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public CodeSalutation whereNotExists(Select<?> select) {
        return where(DSL.notExists(select));
    }
}
