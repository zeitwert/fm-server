// These are needed externally so we put instead in common module to prevent loading session.
export const SESSION_STATE_ITEM = "finadvise-session-state";
export const SESSION_INFO_ITEM = "finadvise-session";
export const AUTH_HEADER_ITEM = "finadvise-auth-header";
