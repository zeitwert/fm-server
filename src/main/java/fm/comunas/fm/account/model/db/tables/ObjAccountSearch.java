/*
 * This file is generated by jOOQ.
 */
package fm.comunas.fm.account.model.db.tables;


import fm.comunas.fm.account.model.db.Public;
import fm.comunas.fm.account.model.db.tables.records.ObjAccountSearchRecord;

import org.jooq.Field;
import org.jooq.ForeignKey;
import org.jooq.Name;
import org.jooq.Record;
import org.jooq.Row3;
import org.jooq.Schema;
import org.jooq.Table;
import org.jooq.TableField;
import org.jooq.TableOptions;
import org.jooq.impl.DSL;
import org.jooq.impl.SQLDataType;
import org.jooq.impl.TableImpl;


/**
 * This class is generated by jOOQ.
 */
@SuppressWarnings({ "all", "unchecked", "rawtypes" })
public class ObjAccountSearch extends TableImpl<ObjAccountSearchRecord> {

    private static final long serialVersionUID = 1L;

    /**
     * The reference instance of <code>public.obj_account_search</code>
     */
    public static final ObjAccountSearch OBJ_ACCOUNT_SEARCH = new ObjAccountSearch();

    /**
     * The class holding records for this type
     */
    @Override
    public Class<ObjAccountSearchRecord> getRecordType() {
        return ObjAccountSearchRecord.class;
    }

    /**
     * The column <code>public.obj_account_search.obj_id</code>.
     */
    public final TableField<ObjAccountSearchRecord, Integer> OBJ_ID = createField(DSL.name("obj_id"), SQLDataType.INTEGER, this, "");

    /**
     * The column <code>public.obj_account_search.tokens</code>.
     */
    public final TableField<ObjAccountSearchRecord, String> TOKENS = createField(DSL.name("tokens"), SQLDataType.CLOB, this, "");

    /**
     * The column <code>public.obj_account_search.text</code>.
     */
    public final TableField<ObjAccountSearchRecord, String> TEXT = createField(DSL.name("text"), SQLDataType.CLOB, this, "");

    private ObjAccountSearch(Name alias, Table<ObjAccountSearchRecord> aliased) {
        this(alias, aliased, null);
    }

    private ObjAccountSearch(Name alias, Table<ObjAccountSearchRecord> aliased, Field<?>[] parameters) {
        super(alias, null, aliased, parameters, DSL.comment(""), TableOptions.view("create view \"obj_account_search\" as  SELECT obj_account_v.obj_id,\n    (((('account '::text || (COALESCE(obj_account_v.name, ' '::character varying))::text) || ' '::text) || (COALESCE(obj_account_v.intl_key, ' '::character varying))::text) || ' '::text) AS tokens,\n    (COALESCE(obj_account_v.description, ' '::text) || ' '::text) AS text\n   FROM obj_account_v;"));
    }

    /**
     * Create an aliased <code>public.obj_account_search</code> table reference
     */
    public ObjAccountSearch(String alias) {
        this(DSL.name(alias), OBJ_ACCOUNT_SEARCH);
    }

    /**
     * Create an aliased <code>public.obj_account_search</code> table reference
     */
    public ObjAccountSearch(Name alias) {
        this(alias, OBJ_ACCOUNT_SEARCH);
    }

    /**
     * Create a <code>public.obj_account_search</code> table reference
     */
    public ObjAccountSearch() {
        this(DSL.name("obj_account_search"), null);
    }

    public <O extends Record> ObjAccountSearch(Table<O> child, ForeignKey<O, ObjAccountSearchRecord> key) {
        super(child, key, OBJ_ACCOUNT_SEARCH);
    }

    @Override
    public Schema getSchema() {
        return Public.PUBLIC;
    }

    @Override
    public ObjAccountSearch as(String alias) {
        return new ObjAccountSearch(DSL.name(alias), this);
    }

    @Override
    public ObjAccountSearch as(Name alias) {
        return new ObjAccountSearch(alias, this);
    }

    /**
     * Rename this table
     */
    @Override
    public ObjAccountSearch rename(String name) {
        return new ObjAccountSearch(DSL.name(name), null);
    }

    /**
     * Rename this table
     */
    @Override
    public ObjAccountSearch rename(Name name) {
        return new ObjAccountSearch(name, null);
    }

    // -------------------------------------------------------------------------
    // Row3 type methods
    // -------------------------------------------------------------------------

    @Override
    public Row3<Integer, String, String> fieldsRow() {
        return (Row3) super.fieldsRow();
    }
}
