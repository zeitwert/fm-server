/*
 * This file is generated by jOOQ.
 */
package io.zeitwert.fm.building.model.db.tables;


import io.zeitwert.fm.building.model.db.Public;
import io.zeitwert.fm.building.model.db.tables.records.ObjBuildingSearchRecord;

import org.jooq.Field;
import org.jooq.ForeignKey;
import org.jooq.Name;
import org.jooq.Record;
import org.jooq.Row4;
import org.jooq.Schema;
import org.jooq.Table;
import org.jooq.TableField;
import org.jooq.TableOptions;
import org.jooq.impl.DSL;
import org.jooq.impl.SQLDataType;
import org.jooq.impl.TableImpl;


/**
 * This class is generated by jOOQ.
 */
@SuppressWarnings({ "all", "unchecked", "rawtypes" })
public class ObjBuildingSearch extends TableImpl<ObjBuildingSearchRecord> {

    private static final long serialVersionUID = 1L;

    /**
     * The reference instance of <code>public.obj_building_search</code>
     */
    public static final ObjBuildingSearch OBJ_BUILDING_SEARCH = new ObjBuildingSearch();

    /**
     * The class holding records for this type
     */
    @Override
    public Class<ObjBuildingSearchRecord> getRecordType() {
        return ObjBuildingSearchRecord.class;
    }

    /**
     * The column <code>public.obj_building_search.obj_id</code>.
     */
    public final TableField<ObjBuildingSearchRecord, Integer> OBJ_ID = createField(DSL.name("obj_id"), SQLDataType.INTEGER, this, "");

    /**
     * The column <code>public.obj_building_search.account_id</code>.
     */
    public final TableField<ObjBuildingSearchRecord, Integer> ACCOUNT_ID = createField(DSL.name("account_id"), SQLDataType.INTEGER, this, "");

    /**
     * The column <code>public.obj_building_search.tokens</code>.
     */
    public final TableField<ObjBuildingSearchRecord, String> TOKENS = createField(DSL.name("tokens"), SQLDataType.CLOB, this, "");

    /**
     * The column <code>public.obj_building_search.text</code>.
     */
    public final TableField<ObjBuildingSearchRecord, String> TEXT = createField(DSL.name("text"), SQLDataType.CLOB, this, "");

    private ObjBuildingSearch(Name alias, Table<ObjBuildingSearchRecord> aliased) {
        this(alias, aliased, null);
    }

    private ObjBuildingSearch(Name alias, Table<ObjBuildingSearchRecord> aliased, Field<?>[] parameters) {
        super(alias, null, aliased, parameters, DSL.comment(""), TableOptions.view("create view \"obj_building_search\" as  SELECT obj_building_v.obj_id,\n    obj_building_v.account_id,\n    (((((((((((((((('building '::text || (COALESCE(obj_building_v.name, ' '::character varying))::text) || ' '::text) || (COALESCE(obj_building_v.building_nr, ' '::character varying))::text) || ' '::text) || (COALESCE(obj_building_v.insurance_nr, ' '::character varying))::text) || ' '::text) || (COALESCE(obj_building_v.plot_nr, ' '::character varying))::text) || ' '::text) || (COALESCE(obj_building_v.national_building_id, ' '::character varying))::text) || ' '::text) || (COALESCE(obj_building_v.street, ' '::character varying))::text) || ' '::text) || (COALESCE(obj_building_v.city, ' '::character varying))::text) || ' '::text) || (COALESCE(obj_building_v.zip, ' '::character varying))::text) || ' '::text) AS tokens,\n    (COALESCE(obj_building_v.description, ' '::text) || ' '::text) AS text\n   FROM obj_building_v;"));
    }

    /**
     * Create an aliased <code>public.obj_building_search</code> table reference
     */
    public ObjBuildingSearch(String alias) {
        this(DSL.name(alias), OBJ_BUILDING_SEARCH);
    }

    /**
     * Create an aliased <code>public.obj_building_search</code> table reference
     */
    public ObjBuildingSearch(Name alias) {
        this(alias, OBJ_BUILDING_SEARCH);
    }

    /**
     * Create a <code>public.obj_building_search</code> table reference
     */
    public ObjBuildingSearch() {
        this(DSL.name("obj_building_search"), null);
    }

    public <O extends Record> ObjBuildingSearch(Table<O> child, ForeignKey<O, ObjBuildingSearchRecord> key) {
        super(child, key, OBJ_BUILDING_SEARCH);
    }

    @Override
    public Schema getSchema() {
        return Public.PUBLIC;
    }

    @Override
    public ObjBuildingSearch as(String alias) {
        return new ObjBuildingSearch(DSL.name(alias), this);
    }

    @Override
    public ObjBuildingSearch as(Name alias) {
        return new ObjBuildingSearch(alias, this);
    }

    /**
     * Rename this table
     */
    @Override
    public ObjBuildingSearch rename(String name) {
        return new ObjBuildingSearch(DSL.name(name), null);
    }

    /**
     * Rename this table
     */
    @Override
    public ObjBuildingSearch rename(Name name) {
        return new ObjBuildingSearch(name, null);
    }

    // -------------------------------------------------------------------------
    // Row4 type methods
    // -------------------------------------------------------------------------

    @Override
    public Row4<Integer, Integer, String, String> fieldsRow() {
        return (Row4) super.fieldsRow();
    }
}
